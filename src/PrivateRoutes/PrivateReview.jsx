
import { useQuery } from '@tanstack/react-query';

const PrivateReview = ({children}) => {

    const {}=useQuery({
        queryKey:
    })
    return (
        <div>
            
        </div>
    );
};

export default PrivateReview;